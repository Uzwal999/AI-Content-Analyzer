{
  "name": "evervfx-ai-brand-content-analyzer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "typecheck": "npm --prefix frontend run typecheck",
    "start": "npm --prefix frontend run start"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}

